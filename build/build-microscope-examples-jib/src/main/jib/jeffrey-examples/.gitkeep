# Placeholder: keeps the directory tracked in git so Jib's extraDirectories has
# a valid source path locally. CI clones https://github.com/petrbouda/jeffrey-recordings.git
# into this directory before jib:build, populating /jeffrey-examples/ in the image.
