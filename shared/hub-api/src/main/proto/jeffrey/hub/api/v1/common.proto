syntax = "proto3";

package jeffrey.hub.api.v1;

option java_package = "cafe.jeffrey.hub.api.v1";
option java_multiple_files = true;

// Shared enums used across multiple gRPC services.

// Recording status — applies to projects, recording sessions, and repository files.
enum RecordingStatus {
  RECORDING_STATUS_UNSPECIFIED = 0;
  // Recording/session is actively collecting data.
  RECORDING_STATUS_ACTIVE = 1;
  // Recording/session has completed.
  RECORDING_STATUS_FINISHED = 2;
  // Status could not be determined.
  RECORDING_STATUS_UNKNOWN = 3;
}

// Instance lifecycle status.
enum InstanceStatus {
  INSTANCE_STATUS_UNSPECIFIED = 0;
  // Instance created, waiting for first session.
  INSTANCE_STATUS_PENDING = 1;
  // Instance has at least one active session.
  INSTANCE_STATUS_ACTIVE = 2;
  // All sessions have finished.
  INSTANCE_STATUS_FINISHED = 3;
  // Instance has been cleaned up after retention period.
  INSTANCE_STATUS_EXPIRED = 4;
}

// Severity level for messages and alerts.
enum Severity {
  SEVERITY_UNSPECIFIED = 0;
  // Critical severity — immediate attention required.
  SEVERITY_CRITICAL = 1;
  // High severity — requires attention.
  SEVERITY_HIGH = 2;
  // Medium severity — should be investigated.
  SEVERITY_MEDIUM = 3;
  // Low severity — informational.
  SEVERITY_LOW = 4;
}

// The scope a published configuration belongs to. The order is the merge order on the shared
// volume: a file in a project's folder overrides the workspace's, which overrides the one in the
// workspaces root. Mirrors cafe.jeffrey.shared.common.config.ConfigScope.
enum ConfigScope {
  CONFIG_SCOPE_UNSPECIFIED = 0;
  CONFIG_SCOPE_GLOBAL = 1;
  CONFIG_SCOPE_WORKSPACE = 2;
  CONFIG_SCOPE_PROJECT = 3;
}

// What a published value configures. Closed on purpose: a value here becomes a JVM option inside
// an application's own argfile, so the set of things that can be said is a security boundary
// rather than a convenience. Mirrors cafe.jeffrey.shared.common.config.ConfigType.
enum ConfigType {
  CONFIG_TYPE_UNSPECIFIED = 0;
  // The async-profiler command a session starts with.
  CONFIG_TYPE_ASPROF_SETTINGS = 1;
}
