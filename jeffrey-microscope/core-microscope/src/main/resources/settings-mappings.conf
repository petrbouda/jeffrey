settings {
  properties {
    logging {
      logging.level.cafe.jeffrey = "INFO"
    }
    visualization {
      jeffrey.microscope.visualization.flamegraph {
        min-frame-threshold-pct = "0.05"
        frame-text-mode = "single-line"
      }
    }
    ai-export {
      jeffrey.microscope.ai-export.flamegraph {
        min-frame-threshold-pct = "1.0"
      }
    }
  }
}
