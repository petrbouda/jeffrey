# Read by the jeffrey-jib extension (cafe.jeffrey.jib.payload.PayloadDescriptor) to learn which
# provisioner build this payload jar carries and where its binaries came from. Filtered by Maven.
kind=native
provisioner.provenance=jeffrey ${jeffrey.release}
profiler.provenance=async-profiler ${async-profiler.version}
