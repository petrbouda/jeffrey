<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Jeffrey
  ~ Copyright (C) 2026 Petr Bouda
  ~
  ~ This program is free software: you can redistribute it and/or modify
  ~ it under the terms of the GNU Affero General Public License as published by
  ~ the Free Software Foundation, either version 3 of the License, or
  ~ (at your option) any later version.
  ~
  ~ This program is distributed in the hope that it will be useful,
  ~ but WITHOUT ANY WARRANTY; without even the implied warranty of
  ~ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  ~ GNU Affero General Public License for more details.
  ~
  ~ You should have received a copy of the GNU Affero General Public License
  ~ along with this program.  If not, see <http://www.gnu.org/licenses/>.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cafe.jeffrey-analyst</groupId>
    <artifactId>jeffrey-jib</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>${project.groupId}:${project.artifactId}</name>
    <description>JIB plugin extensions (Gradle and Maven) that wrap the container entrypoint so Jeffrey profiling initialises before the app starts, without overriding the Kubernetes command.</description>
    <url>https://github.com/petrbouda/jeffrey</url>

    <developers>
        <developer>
            <name>Petr Bouda</name>
            <email>bouda.petr@gmail.com</email>
            <organization>Jeffrey</organization>
            <organizationUrl>https://www.jeffrey-analyst.cafe/</organizationUrl>
        </developer>
    </developers>

    <licenses>
        <license>
            <name>GNU Affero General Public License v3.0</name>
            <url>https://www.gnu.org/licenses/agpl-3.0.html</url>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:https://github.com/petrbouda/jeffrey.git</connection>
        <developerConnection>scm:git:https://github.com/petrbouda/jeffrey.git</developerConnection>
        <url>https://github.com/petrbouda/jeffrey/tree/main</url>
    </scm>

    <modules>
        <module>jeffrey-jib-core</module>
        <module>jeffrey-jib-gradle</module>
        <module>jeffrey-jib-maven</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <jib.extension.version>0.4.0</jib.extension.version>
        <maven-core.version>3.9.9</maven-core.version>
        <maven-resolver.version>1.9.18</maven-resolver.version>
        <junit.version>5.14.3</junit.version>
        <maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>
        <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
        <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
        <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
        <!-- The async-profiler release whose libasyncProfiler.so the payload modules bundle.
             Pinned on purpose: this library is loaded into every profiled JVM, so which version
             ships is a decision, not whatever happened to be latest on the day of the build. -->
        <async-profiler.version>4.1</async-profiler.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>jeffrey-jib-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-gradle-plugin-extension-api</artifactId>
                <version>${jib.extension.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin-extension-api</artifactId>
                <version>${jib.extension.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-core</artifactId>
                <version>${maven-core.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-api</artifactId>
                <version>${maven-resolver.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!--
          The payload modules package binaries that CI stages from a published Jeffrey release and
          from async-profiler. They are built only when those files are present, so an ordinary
          `mvn test` of the extension itself needs no downloads.
        -->
        <profile>
            <id>payload</id>
            <modules>
                <module>jeffrey-jib-payload-native</module>
                <module>jeffrey-jib-payload-jar</module>
                <module>jeffrey-jib-payload-profiler</module>
            </modules>
        </profile>
        <profile>
            <id>release</id>
            <!--
              Staging-only release profile. Maven produces each module's jar + sources +
              javadoc and deploys them into a local staging repository
              (target/staging-deploy, via -DaltDeploymentRepository). Signing and the
              actual upload to Maven Central are handled by JReleaser (`jreleaser deploy`,
              see jreleaser.yml), which replaced central-publishing-maven-plugin +
              maven-gpg-plugin.
            -->
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.0</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.6.3</version>
                        <executions>
                            <execution>
                                <id>attach-javadoc</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <stylesheet>java</stylesheet>
                            <doclint>none</doclint>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
